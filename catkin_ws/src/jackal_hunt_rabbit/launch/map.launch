<launch>

  <arg name="map_file" default="$(find jackal_hunt_rabbit)/maps/map.yaml"/>

  <!-- Run the map server -->
  <node name="map_server" pkg="map_server" type="map_saver" args="$(arg map_file)" />

</launch>
